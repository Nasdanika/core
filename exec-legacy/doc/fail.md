Fails execution with a supplied message. Message (fail value) shall be a content component or a list of components.
Can be used in control flow components such as switch or if.

### Example

```yaml
fail: ${message}
```